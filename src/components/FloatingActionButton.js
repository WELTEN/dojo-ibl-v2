import glamorous from 'glamorous';
import MuiFloatingActionButton from 'material-ui/FloatingActionButton';

const FloatingActionButton = glamorous(MuiFloatingActionButton)({
  position: 'fixed',
  right: 24,
  bottom: 24
});

export default FloatingActionButton;
